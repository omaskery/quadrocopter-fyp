#ifndef INIT_H
#define INIT_H

// enables interrupts to occur
void InterruptsEnable(void);
// performs initialisation of system (application specific, not reusable)
void InitialiseSystem(void);

#endif
